import React from 'react';
import { HashRouter } from 'react-router-dom';
import App from '../browser/screens/App';

const Root = () => (
  <HashRouter>
    <App />
  </HashRouter>
)

export default Root;
